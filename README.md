# VCC-Assignment-1
Python flask code to run on one VM and access from another VM
# Python Flask Microservice

## Description
A simple RESTful microservice deployed on a virtual machine using Flask.

## How to Run
1. Install Python 3
2. Install Flask: `pip install flask`
3. Run: `python3 app.py`

## API Endpoint
GET /api/hello

